<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/authentication_background"
    tools:context=".views.activities.AuthActivity">

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/LA_authentication"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginVertical="50dp"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/cd_sign_up"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:lottie_rawRes="@raw/credentials" />

    <FrameLayout
        android:id="@+id/FL_authenticationFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/LA_authentication" />

</RelativeLayout>
